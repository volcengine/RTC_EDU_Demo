// import { TOOL_TYPE } from '@volcengine/white-board-manage';

export enum PopType {
  textbox = 0,
  pencilBrush = 1,
  shape = 2,
  background = 3,
}

// export type IToolMode = TOOL_TYPE | PaintConstant | '';
